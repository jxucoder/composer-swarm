{
  "name": "composer-swarm",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Repo-local CLI that launches Cursor/Composer worker teams from Claude Code, Codex, or a shell.",
  "bin": {
    "composer-swarm": "./bin/composer-swarm.mjs"
  },
  "engines": {
    "node": ">=18.18.0"
  },
  "scripts": {
    "doctor": "node bin/composer-swarm.mjs doctor",
    "test": "node --test tests/*.test.mjs"
  }
}
