{
  "name": "composer-swarm",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "Add fast, low-cost Composer worker teams to Claude Code or Codex.",
  "bin": {
    "composer-swarm": "./bin/composer-swarm.mjs"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "doctor": "node bin/composer-swarm.mjs doctor",
    "test": "node --test tests/*.test.mjs"
  }
}
